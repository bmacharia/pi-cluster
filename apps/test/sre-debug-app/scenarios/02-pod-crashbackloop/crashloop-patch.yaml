apiVersion: apps/v1
kind: Deployment
metadata:
  name: sre-debug-app
  namespace: sre-debug
spec:
  template:
    spec:
      containers:
        - name: app
          command: ["/bin/sh", "-c"]
          args:
            - echo "Simulated crash for SRE debugging lab"; exit 1
            