apiVersion: apps/v1
kind: Deployment
metadata:
  name: sre-debug-app
  namespace: sre-debug
spec:
  template:
    spec:
      containers:
        - name: app
          image: ghcr.io/bmacharia/this-image-does-not-exist:lesson-1

