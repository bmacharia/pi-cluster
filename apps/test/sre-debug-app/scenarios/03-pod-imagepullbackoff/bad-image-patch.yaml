apiVersion: apps/v1
Kind: Deployment
metadata:
  name: sre-debug
  namespace: sre-debug
spec:
  template:
    spec:
      containers: 
        - name: app
        - image: ghcr.io/bmacharia/this-image-does-not-exist:lesson-1

